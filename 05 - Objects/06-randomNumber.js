// Generate Random Numbers:

const random = Math.random();

console.log(random);  // decimal between 0-1

console.log(Math.round(random * 100));  // decimal between 1 - 100

// --> There are many other objects, such as the Date Object, we will cover later on.
// ------------------------------------------------------------------------------
